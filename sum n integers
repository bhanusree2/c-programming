#include <stdio.h>

int main() {
    int i = 1, sum = 0, n;
    printf("Enter the value for n: ");
    scanf("%d", &n);
    while (i <= n) {
        sum = sum + i;
        i++;
    }
    printf("The sum of %d numbers is %d", n, sum);
    return 0;
}
