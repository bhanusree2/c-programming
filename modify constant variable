#include <stdio.h>

int main() {
    const int num = 10;
    int *ptr = (int*)&num;

    printf("Before modification: num = %d\n", num);

    *ptr = 20;

    printf("After modification: num = %d\n", num);

    return 0;
}

